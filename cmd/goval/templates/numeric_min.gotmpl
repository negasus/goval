    { // {{ .field }}
        if model.{{ .field }} < {{ .min }} {
            errors["{{ .name }}"] = append(errors["{{ .name }}"], goval.Error{
                Type: goval.ErrorTypeMinNumeric,
                Values: map[string]any{
                    "field": "{{ .name }}",
                    "min":   {{ .min }},
                },
            })
        }
    }
